---
// The full archive: every published post, newest first, across all sections.
import ListingLayout from '../../layouts/ListingLayout.astro';
import { getPublishedPosts } from '../../lib/posts';

const posts = await getPublishedPosts();
---

<ListingLayout heading="All posts" blurb="Everything, newest first." posts={posts} />
